import { PermissionIf } from './permission-if.directive';

describe('PermissionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionIf();
    expect(directive).toBeTruthy();
  });
});
